# Tunnel Mesh 设计规范 v2.0

## 一、降秩后的概念体系

### 两个生成器

| 生成器 | 定义 | 不变属性 |
|--------|------|----------|
| **主仆关系** | 主发起控制，仆被控制 | 只有这一种关系 |
| **触达方式** | 主能否直接 TCP 到仆的 SSH 端口 | 决定连接方式 |

### 触达方式决定了连接方案

| 触达方式 | 条件 | 连接方案 | 维持方式 |
|----------|------|----------|----------|
| **正向** | 主能 TCP 到仆 | 直接 SSH | SSH 密钥即可 |
| **反向** | 仆能 TCP 到主，主不能到仆 | 反向隧道 | 常驻 SSH -R 进程 |
| **不可达** | 双方都不能互连 | 需要中间节点 | 通过路径解决 |

### 概念消除清单

| 旧概念 | 为什么消除 | 新体系中 |
|--------|-----------|----------|
| 平等契约 | 就是两组互相的主仆关系 | A→B + B→A |
| 主仆契约 | 就是一组主仆关系 | 唯一的关系 |
| 契约之塔/中转/跳板 | 都是路径上的中间节点 | 普通服务器，恰好有公网IP |
| 桥 | 替仆建立隧道的第三方 | 普通服务器，恰好能访问双方 |

---

## 二、核心功能设计

### 功能矩阵

```
┌─────────────────────────────────────────────────────────┐
│                    主菜单                                │
├─────────────────────────────────────────────────────────┤
│  [1] 缔结契约    - 建立新的主仆关系                      │
│  [2] 审视契约    - 查看我所有的仆                        │
│  [3] 追寻仆人    - 查看我的仆的仆（多级探索）            │
│  [4] 连接仆人    - 连接到某个仆人（自动选路径）           │
│  [5] 断契        - 删除某条主仆关系                      │
│  [6] 契约之仪    - 启动所有反向隧道                      │
│  [7] 密钥保管    - 管理 SSH 密钥                         │
│  [8] 契约法典    - 帮助                                  │
│  [Q] 退出                                                 │
└─────────────────────────────────────────────────────────┘
```

### 子功能详解

#### [1] 缔结契约

```
步骤：
  1. 选择触达方式：
     [1] 正向 - 我能直接连到仆的 SSH 端口
     [2] 反向 - 仆能连到我，但我不能连仆
     [3] 通过路径 - 我通过已有的仆人间接连接新仆

  2. 输入仆人信息：
     - 名称
     - IP:端口
     - 登录用户

  3. 如果是反向：
     分配监听端口（自动避免冲突）

  4. 如果是通过路径：
     自动查找路径，生成 ProxyJump 配置

  5. 生成契约文书（可复制给对方）
```

#### [3] 追寻仆人（传递可达性）

```
输入：想访问的目标服务器名称
输出：路径（如果可达）

示例：
  $ 追寻仆人：node3

  路径分析：
    你(Windows) → 阿里云 [正向，直接可达]
    阿里云     → node3  [反向，隧道可达]

  可用路径：
    Windows → 阿里云 → node3
    （通过 ProxyJump：ssh -J 阿里云 node3）

  是否将此路径保存为契约？[y/N]
```

#### [4] 连接仆人

```
输入：仆人名称
自动选择最优路径（最少跳数）

  你(Windows) → 阿里云 → node3
    ssh -J 阿里云 node3

  或如果直接可达：
    ssh node3
```

---

## 三、数据结构

### 契约（Contract）

```json
{
  "contracts": [
    {
      "id": "aliyun-from-windows",
      "master": "windows",
      "servant": "aliyun", 
      "servant_ip": "8.131.61.234",
      "servant_port": 22,
      "servant_user": "root",
      "reach": "forward",
      "key": "id_aliyun",
      "status": "active"
    },
    {
      "id": "node3-from-aliyun",
      "master": "aliyun",
      "servant": "node3",
      "servant_ip": "10.16.82.202",
      "servant_port": 5122,
      "servant_user": "wuxiaoran",
      "reach": "reverse",
      "tunnel_port": 2201,
      "tunnel_bridge": "windows",
      "key": "id_node3",
      "status": "active"
    },
    {
      "id": "node3-from-windows",
      "master": "windows",
      "servant": "node3",
      "servant_ip": "10.16.82.202",
      "servant_port": 5122,
      "servant_user": "wuxiaoran",
      "reach": "proxyjump",
      "via": ["aliyun"],
      "key": "id_node3",
      "status": "active"
    }
  ]
}
```

### 全局拓扑（Topology）

```json
{
  "servers": {
    "windows": {"ip": "10.16.73.249", "role": "master"},
    "aliyun": {"ip": "8.131.61.234", "role": "relay"},
    "node3": {"ip": "10.16.82.202", "role": "servant"}
  },
  "edges": [
    {"from": "windows", "to": "aliyun", "reach": "forward"},
    {"from": "aliyun", "to": "node3", "reach": "reverse", "port": 2201},
    {"from": "windows", "to": "node3", "reach": "proxyjump", "via": ["aliyun"]}
  ]
}
```

---

## 四、端口冲突解决方案

反向隧道需要端口。三台服务器以上可能出现冲突。

```
策略：

  1. 每台「主」维护自己的端口池
     端口池 = 2201-2299（99个端口，足够）
  
  2. 分配算法：
     按契约创建顺序从 2201 开始
     删除契约时释放端口
  
  3. 冲突检测：
     立契时检查端口是否被占用
     已占用则向后找下一个可用端口
  
  4. 跨服务器不冲突：
     Windows 的反向隧道端口和 阿里云 的反向隧道端口
     在不同物理机上，端口可以相同，不会冲突
     
     真正冲突的是：同一台「主」上有多个仆的反向隧道
     例如 阿里云 既是 node3 的主也是 server4 的主
     两个反向隧道都在阿里云上监听，端口不能一样
```

---

## 五、密钥管理

```
每个主仆关系可以有自己的密钥，也可以共享。

结构：
  ~/.ssh/tunnel-mesh/
  ├── keys.json              # 密钥索引
  ├── id_windows_aliyun      # Windows→阿里云 专用
  ├── id_windows_aliyun.pub
  ├── id_aliyun_node3        # 阿里云→node3 专用
  └── id_aliyun_node3.pub

keys.json:
  {
    "id_windows_aliyun": {
      "contract": "aliyun-from-windows",
      "type": "ed25519",
      "fingerprint": "SHA256:xxx",
      "created": "2026-05-15"
    }
  }
```

---

## 六、使用样例

### 样例 1：你的实际场景完整流程

```
场景：三台服务器，建立四对主仆关系

═════════════════════════════════════════════════════
Step 1：Windows → 阿里云（正向）
═════════════════════════════════════════════════════

在 Windows 上：
  [1] 缔结契约
  选择 [1] 正向
  仆人名称: aliyun
  IP:端口: 8.131.61.234:22
  用户: root
  密钥: 生成新密钥 / 选择已有

  结果：
  ✓ 密钥: ~/.ssh/id_windows_aliyun
  ✓ SSH config: Host aliyun → 8.131.61.234:22
  ✓ 测试通过

═════════════════════════════════════════════════════
Step 2：Windows → node3（正向，同校园网）
═════════════════════════════════════════════════════

在 Windows 上：
  [1] 缔结契约
  选择 [1] 正向
  仆人名称: node3
  IP:端口: 10.16.82.202:5122
  用户: wuxiaoran
  密钥: 生成新密钥

  结果：
  ✓ 密钥: ~/.ssh/id_windows_node3
  ✓ SSH config: Host node3 → 10.16.82.202:5122
  ✓ 测试通过

═════════════════════════════════════════════════════
Step 3：阿里云 → node3（反向，需隧道）
═════════════════════════════════════════════════════

在阿里云上：
  [1] 缔结契约
  选择 [2] 反向
  仆人名称: node3
  IP:端口: 10.16.82.202:5122
  用户: wuxiaoran
  
  系统检测到阿里云不能直连 10.16.82.202
  自动搜索路径：
    阿里云 → Windows → node3 ✓ 存在路径！
  
  隧道将通过 Windows 建立
  分配监听端口: 2201
  生成契约文书：

  ┌──────────────────────────────────────┐
  │ ===CONTRACT===                       │
  │ REACH=reverse                        │
  │ MASTER=aliyun                        │
  │ SERVANT=node3                        │
  │ SERVANT_IP=10.16.82.202              │
  │ SERVANT_PORT=5122                    │
  │ TUNNEL_PORT=2201                     │
  │ BRIDGE=windows                       │
  │ ===END===                            │
  └──────────────────────────────────────┘

  将此文书复制到 Windows

在 Windows 上：
  [1] 缔结契约 → 粘贴文书
  自动创建隧道服务：
    ssh -R 2201:node3:5122 root@aliyun
  自动设为开机启动

═════════════════════════════════════════════════════
Step 4：自动发现传递控制
═════════════════════════════════════════════════════

在 Windows 上：
  [3] 追寻仆人
  输入: node3

  路径分析：
    Windows → 阿里云 [正向，已有]
    Windows → node3   [正向，已有]
    阿里云 → node3   [反向，已有]

  你现在可以通过以下方式连接到 node3：
    路径1: ssh node3              (直连，正向)
    路径2: ssh -J aliyun node3    (通过阿里云，备用)

  [4] 连接仆人 → 选 node3 → 自动选最优路径
```

---

### 样例 2：新人接入现有网络

```
场景：运维新人小李，拿到电脑，需要能管理所有服务器

前置：网络管理员（你）已经把拓扑配置同步到共享位置

小李的 Windows 上：
  1. 安装 Tunnel Mesh
  2. 导入拓扑文件（从共享位置复制）
  3. [3] 追寻仆人 → 全部可达！

  小李不需要了解 tunnel、端口、反向隧道。
  只需要知道 "ssh node3" 就能连。
```

---

### 样例 3：多级传递

```
场景：A → B → C → D

  A(家里) → B(阿里云) [正向]
  B(阿里云) → C(公司内网) [反向]
  C(公司内网) → D(实验室服务器) [正向]

在 A 上：
  [3] 追寻仆人 → D

  路径分析：
    A → B [正向]
    B → C [反向，需隧道]
    C → D [正向]

  结果：
    ✓ 路径：A → B → C → D
    ssh -J B,C D

  是否保存？[y/N]
  保存后：ssh D 即可（自动 ProxyJump B,C）
```

---

### 样例 4：端口冲突解决

```
场景：阿里云需要同时连接 node3 和 server4

  契约1：阿里云 → node3   [反向，端口 2201]
  契约2：阿里云 → server4  [反向，端口 2202]

  系统自动分配不同端口，不会冲突。

如果端口 2201 已被占用（非 tunnel-mesh 的进程）：
  [警告] 端口 2201 已被占用
  [建议] 使用端口 2203
  是否使用建议端口？[Y/n]
```

---

## 七、实现优先级

### 必须实现（P0）

| 功能 | 说明 |
|------|------|
| 缔结契约（正向） | 直接 SSH 配置 |
| 缔结契约（反向） | 反向隧道配置 |
| 审视契约 | 列出所有仆 |
| 连接仆人 | 自动选最优路径 |
| 契约之仪 | 启动所有反向隧道 |
| 断契 | 删除契约（停隧道+删配置+删密钥） |

### 应该实现（P1）

| 功能 | 说明 |
|------|------|
| 追寻仆人 | 多级探索 |
| 通过路径缔结 | ProxyJump 自动配置 |
| 端口冲突检测 | 分配前检查 |
| 密钥管理 | 独立密钥索引 |

### 可以后续（P2）

| 功能 | 说明 |
|------|------|
| 拓扑同步 | 多机拓扑文件共享 |
| 连接测试 | 一键测试可达性 |
| 隧道健康监控 | 进程存活检测 |
